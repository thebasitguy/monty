#include "monty.h"

/**
 * stack_nop - performs a no-operation (nop) for a
 * stack-based interpreter
 *
 * @head: stack head
 * @counter: line_number
 *
 * Return: nothing
 */

void stack_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
